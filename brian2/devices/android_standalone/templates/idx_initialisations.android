{% macro java_code() %}

        int[] {{codeobj_name}}_idx = new int[{{N}}];
        Allocation {{codeobj_name}}_idx_alloc = Allocation.createSized(mRS, Element.I32(mRS), {{N}});
        Allocation {{codeobj_name}}_out_alloc = Allocation.createSized(mRS, Element.I32(mRS), {{N}});
        for (int idx=0; idx<{{N}}; idx++){
            {{codeobj_name}}_idx[idx] = idx;
        }
        {{codeobj_name}}_idx_alloc.copyFrom({{codeobj_name}}_idx);
{% endmacro %}

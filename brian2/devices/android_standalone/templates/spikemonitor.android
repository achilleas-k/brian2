{% macro monitor_declaration() %}
        {% if owner is defined %}
        SpikeMonitor {{owner.source.name}}_spikemonitor = new SpikeMonitor();
        {% endif %}
{% endmacro %}

{% macro java_code() %}

            {% if owner is defined %}
            {{owner.source.name}}_spikemonitor.addSpikes(_array_{{owner.source.name}}__spikespace.clone(), t);
            {% endif %}

{% endmacro %}

